import React from 'react';

const Header = () => {
    return (
        <nav>
            <div className="nav-wrapper light-blue darken-2">
                <span className="brand-logo">¿Qué tiempo hace?</span>
            </div>
        </nav>
    );
};

export default Header;